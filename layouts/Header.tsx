import styled from "@emotion/styled";

export default function Header() {
  return <Wrapper>sds</Wrapper>;
}

const Wrapper = styled.div`
  width: 1024px;
  margin: 0 auto;
  border: 1px solid black;
`;
